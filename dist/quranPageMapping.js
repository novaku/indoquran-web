"use strict";
/**
 * This file provides a mapping between Quran page numbers and surah IDs
 * Since the relationship between pages and surahs is complex (some pages contain multiple surahs
 * and some surahs span multiple pages), this is a simplified mapping
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapPageToSurah = mapPageToSurah;
// Make this work in both browser and Node.js environments
var isClient = typeof window !== 'undefined';
// A more accurate approach would involve a complete mapping of page ranges to surahs
// or even page numbers to ayat ranges within surahs
/**
 * Maps a page number to the most likely surah ID that appears on that page
 * @param pageNumber The page number in the Quran (1-604)
 * @returns The surah ID (1-114) most likely to appear on that page
 */
function mapPageToSurah(pageNumber) {
    // Ensure valid page range
    if (pageNumber < 1)
        return 1;
    if (pageNumber > 604)
        return 114;
    // Approximate mapping based on standard 604-page Quran
    // This is a simplified mapping and may not be 100% accurate for all page renditions
    if (pageNumber === 1)
        return 1; // Al-Fatihah
    if (pageNumber <= 21)
        return 2; // Al-Baqarah (2)
    if (pageNumber <= 41)
        return 3; // Ali 'Imran (3)
    if (pageNumber <= 60)
        return 4; // An-Nisa (4)
    if (pageNumber <= 77)
        return 5; // Al-Ma'idah (5)
    if (pageNumber <= 94)
        return 6; // Al-An'am (6)
    if (pageNumber <= 111)
        return 7; // Al-A'raf (7)
    if (pageNumber <= 120)
        return 8; // Al-Anfal (8)
    if (pageNumber <= 134)
        return 9; // At-Tawbah (9)
    if (pageNumber <= 142)
        return 10; // Yunus (10)
    if (pageNumber <= 151)
        return 11; // Hud (11)
    if (pageNumber <= 160)
        return 12; // Yusuf (12)
    if (pageNumber <= 170)
        return 13; // Ar-Ra'd (13)
    if (pageNumber <= 177)
        return 14; // Ibrahim (14)
    if (pageNumber <= 185)
        return 15; // Al-Hijr (15)
    if (pageNumber <= 197)
        return 16; // An-Nahl (16)
    if (pageNumber <= 208)
        return 17; // Al-Isra (17)
    if (pageNumber <= 219)
        return 18; // Al-Kahf (18)
    if (pageNumber <= 229)
        return 19; // Maryam (19)
    if (pageNumber <= 239)
        return 20; // Ta-Ha (20)
    if (pageNumber <= 251)
        return 21; // Al-Anbiya (21)
    if (pageNumber <= 262)
        return 22; // Al-Hajj (22)
    if (pageNumber <= 271)
        return 23; // Al-Mu'minun (23)
    if (pageNumber <= 281)
        return 24; // An-Nur (24)
    if (pageNumber <= 290)
        return 25; // Al-Furqan (25)
    if (pageNumber <= 300)
        return 26; // Ash-Shu'ara (26)
    if (pageNumber <= 310)
        return 27; // An-Naml (27)
    if (pageNumber <= 320)
        return 28; // Al-Qasas (28)
    if (pageNumber <= 328)
        return 29; // Al-Ankabut (29)
    if (pageNumber <= 335)
        return 30; // Ar-Rum (30)
    if (pageNumber <= 341)
        return 31; // Luqman (31)
    if (pageNumber <= 346)
        return 32; // As-Sajdah (32)
    if (pageNumber <= 355)
        return 33; // Al-Ahzab (33)
    if (pageNumber <= 364)
        return 34; // Saba (34)
    if (pageNumber <= 371)
        return 35; // Fatir (35)
    if (pageNumber <= 378)
        return 36; // Ya-Sin (36)
    if (pageNumber <= 385)
        return 37; // As-Saffat (37)
    if (pageNumber <= 396)
        return 38; // Sad (38)
    if (pageNumber <= 405)
        return 39; // Az-Zumar (39)
    if (pageNumber <= 414)
        return 40; // Ghafir (40)
    if (pageNumber <= 422)
        return 41; // Fussilat (41)
    if (pageNumber <= 430)
        return 42; // Ash-Shura (42)
    if (pageNumber <= 437)
        return 43; // Az-Zukhruf (43)
    if (pageNumber <= 442)
        return 44; // Ad-Dukhan (44)
    if (pageNumber <= 447)
        return 45; // Al-Jathiyah (45)
    if (pageNumber <= 452)
        return 46; // Al-Ahqaf (46)
    if (pageNumber <= 458)
        return 47; // Muhammad (47)
    if (pageNumber <= 464)
        return 48; // Al-Fath (48)
    if (pageNumber <= 468)
        return 49; // Al-Hujurat (49)
    if (pageNumber <= 473)
        return 50; // Qaf (50)
    if (pageNumber <= 477)
        return 51; // Adh-Dhariyat (51)
    if (pageNumber <= 481)
        return 52; // At-Tur (52)
    if (pageNumber <= 485)
        return 53; // An-Najm (53)
    if (pageNumber <= 489)
        return 54; // Al-Qamar (54)
    if (pageNumber <= 493)
        return 55; // Ar-Rahman (55)
    if (pageNumber <= 498)
        return 56; // Al-Waqi'ah (56)
    if (pageNumber <= 504)
        return 57; // Al-Hadid (57)
    if (pageNumber <= 508)
        return 58; // Al-Mujadilah (58)
    if (pageNumber <= 513)
        return 59; // Al-Hashr (59)
    if (pageNumber <= 516)
        return 60; // Al-Mumtahanah (60)
    if (pageNumber <= 519)
        return 61; // As-Saff (61)
    if (pageNumber <= 522)
        return 62; // Al-Jumu'ah (62)
    if (pageNumber <= 525)
        return 63; // Al-Munafiqun (63)
    if (pageNumber <= 528)
        return 64; // At-Taghabun (64)
    if (pageNumber <= 531)
        return 65; // At-Talaq (65)
    if (pageNumber <= 534)
        return 66; // At-Tahrim (66)
    if (pageNumber <= 538)
        return 67; // Al-Mulk (67)
    if (pageNumber <= 542)
        return 68; // Al-Qalam (68)
    if (pageNumber <= 546)
        return 69; // Al-Haqqah (69)
    if (pageNumber <= 549)
        return 70; // Al-Ma'arij (70)
    if (pageNumber <= 552)
        return 71; // Nuh (71)
    if (pageNumber <= 554)
        return 72; // Al-Jinn (72)
    if (pageNumber <= 557)
        return 73; // Al-Muzzammil (73)
    if (pageNumber <= 559)
        return 74; // Al-Muddaththir (74)
    if (pageNumber <= 561)
        return 75; // Al-Qiyamah (75)
    if (pageNumber <= 563)
        return 76; // Al-Insan (76)
    if (pageNumber <= 566)
        return 77; // Al-Mursalat (77)
    if (pageNumber <= 568)
        return 78; // An-Naba (78)
    if (pageNumber <= 570)
        return 79; // An-Nazi'at (79)
    if (pageNumber <= 572)
        return 80; // Abasa (80)
    if (pageNumber <= 574)
        return 81; // At-Takwir (81)
    if (pageNumber <= 575)
        return 82; // Al-Infitar (82)
    if (pageNumber <= 577)
        return 83; // Al-Mutaffifin (83)
    if (pageNumber <= 578)
        return 84; // Al-Inshiqaq (84)
    if (pageNumber <= 580)
        return 85; // Al-Buruj (85)
    if (pageNumber <= 581)
        return 86; // At-Tariq (86)
    if (pageNumber <= 582)
        return 87; // Al-A'la (87)
    if (pageNumber <= 583)
        return 88; // Al-Ghashiyah (88)
    if (pageNumber <= 585)
        return 89; // Al-Fajr (89)
    if (pageNumber <= 586)
        return 90; // Al-Balad (90)
    if (pageNumber <= 587)
        return 91; // Ash-Shams (91)
    if (pageNumber <= 588)
        return 92; // Al-Layl (92)
    if (pageNumber <= 589)
        return 93; // Ad-Duha (93)
    if (pageNumber <= 590)
        return 94; // Ash-Sharh (94)
    if (pageNumber <= 591)
        return 95; // At-Tin (95)
    if (pageNumber <= 592)
        return 96; // Al-Alaq (96)
    if (pageNumber <= 593)
        return 97; // Al-Qadr (97)
    if (pageNumber <= 594)
        return 98; // Al-Bayyinah (98)
    if (pageNumber <= 595)
        return 99; // Az-Zalzalah (99)
    if (pageNumber <= 596)
        return 100; // Al-Adiyat (100)
    if (pageNumber <= 597)
        return 101; // Al-Qari'ah (101)
    if (pageNumber <= 598)
        return 102; // At-Takathur (102)
    if (pageNumber <= 599)
        return 103; // Al-Asr (103)
    if (pageNumber <= 600)
        return 104; // Al-Humazah (104)
    if (pageNumber <= 601)
        return 105; // Al-Fil (105)
    if (pageNumber <= 602)
        return 106; // Quraysh (106)
    if (pageNumber <= 602)
        return 107; // Al-Ma'un (107)
    if (pageNumber <= 603)
        return 108; // Al-Kawthar (108)
    if (pageNumber <= 603)
        return 109; // Al-Kafirun (109)
    if (pageNumber <= 603)
        return 110; // An-Nasr (110)
    if (pageNumber <= 603)
        return 111; // Al-Masad (111)
    if (pageNumber <= 604)
        return 112; // Al-Ikhlas (112)
    if (pageNumber <= 604)
        return 113; // Al-Falaq (113)
    if (pageNumber <= 604)
        return 114; // An-Nas (114)
    // Default to surah 1 if somehow not matched
    return 1;
}
// For CommonJS environments (Node.js)
if (typeof module !== 'undefined' && module.exports) {
    module.exports = { mapPageToSurah: mapPageToSurah };
}
